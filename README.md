# city

## Apt staging sandbox

Empty shell + grass catalog (furniture/cars/arch) near `Large_3` (`window.__cityAptExample`). Drag catalog pieces onto the room floor (or click / `addFromCatalog`); drag in-room pieces to slide; **scroll wheel while dragging** raises/lowers height (scroll up → raise, clamped 0–2.5 m; camera zoom/fly-speed unchanged). See `docs/specs/05-apartments.md`.

Loft sofa: extract albedo ≥1024 PNG (`scripts/extract-loft-furniture.mjs`); MeshBasic color lift — fabric cushions, not black chassis.
