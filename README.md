# city

## Apt staging sandbox

Empty shell + loft street catalog near `Large_3` (`window.__cityAptExample`). Drag catalog pieces onto the room floor (or click / `addFromCatalog`); drag in-room pieces to slide. See `docs/specs/05-apartments.md`.

